<?xml version='1.0' encoding='utf-8'?>
<!-- android-versionCode is an attribute of <widget>, not a <preference>: cordova reads
     it with getAttribute(). Pinned, and no longer derived from the version beside it.

     Cordova would otherwise compute MAJOR*10000 + MINOR*100 + PATCH, so 0.6.0 gives 600
     - below the 10000 that 1.0.0 produced and that is already installed on phones, and
     Android refuses to install a lower code. Hence the +10000 offset: 0.7.0 becomes
     10700, and so on.

     It must therefore be bumped by hand at every release, and only ever upwards. Watch
     out when reaching 1.x: 1.0.0 would derive 10000 and be a downgrade. -->
<widget id="fr.inria.cesam" version="0.6.0" android-versionCode="10600" xmlns="http://www.w3.org/ns/widgets" xmlns:android="http://schemas.android.com/apk/res/android" xmlns:cdv="http://cordova.apache.org/ns/1.0">
    <name>CESAM</name>
    <description>
        CESAM  application
    </description>
    <author href="https://project.inria.fr/humanlabinria/">
        HumanLab Inria team
    </author>
    <icon src="www/img/logoCESAM.png" />
    <content src="index.html" />
    <access origin="*" />
    <preference name="android-minSdkVersion" value="24" />
    <preference name="android-targetSdkVersion" value="34" />
    <preference name="android-compileSdkVersion" value="34" />
    <platform name="android">
        <!-- BLE permissions are declared here rather than by the plugin: we use the "slim"
             variant of cordova-plugin-ble-central, which ships no Android permission at all,
             so that we can set the neverForLocation flag on BLUETOOTH_SCAN.

             Without that flag, Android 12+ (API 31+) only delivers BLE scan results if the app
             also holds a location permission. ACCESS_FINE/COARSE_LOCATION are capped at
             maxSdkVersion="30", so from Android 12 on the app holds none of them and the scan
             silently returns zero result - no error, no onScanFailed.
             See https://developer.android.com/guide/topics/connectivity/bluetooth/permissions -->
        <config-file target="AndroidManifest.xml" parent="/manifest">
            <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="28" />
            <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
            <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
            <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
            <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
            <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
        </config-file>
    </platform>
</widget>
